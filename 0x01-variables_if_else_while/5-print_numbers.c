#include <stdio.h>
/**
 * main - Prints all single bit numbers from 0 to 9
 * Return: always 0
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);

}

