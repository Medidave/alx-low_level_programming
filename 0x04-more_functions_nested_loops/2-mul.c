#include "main.h"

/**
 * mul - multiplies two integers
 * @x: the first number to multiply
 * @y: the second number to multiply the first number
 * Return: The result between the two multiplication
 */

int mul(int x, int y)
{
	return (x * y);
}

