#include "main.h"
/**
 * _isupper  - checks if a letter is upper
 * Return: 1 for upper letter and 0 for any else
 */

int _isupper(int c)
{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
